{
  "name": "mcs",
  "version": "1.0.0",
  "description": "Mocha, Chai and Sinonjs chapter",
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive",
    "watch": "mocha --recursive --watch",
    "grep-hooks": "DEBUG=hooks mocha --recursive --grep hooks",
    "tdd-style": "mocha --recursive --ui tdd --grep TDD"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.4.0",
    "debug": "^2.2.0",
    "mocha": "^2.3.3",
    "sinon": "^1.17.2",
    "sinon-chai": "^2.8.0"
  }
}
